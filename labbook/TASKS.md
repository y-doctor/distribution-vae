# Task Board

## TODO
- [ ] Implement `dist_vae/losses.py` — S — no dependencies
- [ ] Implement `tests/test_losses.py` — S — depends on losses.py
- [ ] Implement `dist_vae/data.py` — M — no code dependencies
- [ ] Implement `tests/test_data.py` — S — depends on data.py
- [ ] Implement `dist_vae/model.py` — L — depends on losses.py
- [ ] Implement `tests/test_model.py` — M — depends on model.py
- [ ] Implement `dist_vae/train.py` — M — depends on model, data, losses
- [ ] Implement `dist_vae/eval.py` — M — depends on model, data
- [ ] Implement `scripts/download_sample_data.py` — S — standalone
- [ ] Implement `scripts/train.py` — S — depends on train module
- [ ] Implement `scripts/encode_dataset.py` — S — depends on model, data
- [ ] Implement `scripts/evaluate.py` — S — depends on eval module
- [ ] Create `notebooks/quickstart.ipynb` — S — depends on everything
- [ ] End-to-end test: synthetic data → train → encode → eval — M
- [ ] End-to-end test: Norman et al. → train → encode → eval — M
- [ ] Write README.md content — S

## IN PROGRESS
(none)

## DONE
(none)
